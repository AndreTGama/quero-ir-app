import 'package:flutter/material.dart';
import 'package:quero_ir_app/Components/backgorund_second_model.dart';

class Body extends StatelessWidget {
  const Body({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const BackgroundSecondModel(child: Column(),);
  }
}
